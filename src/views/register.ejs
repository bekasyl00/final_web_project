<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>PulseBridge | Register</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;600;700&display=swap" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body>
    <nav class="navbar navbar-expand-lg sticky-top border-bottom">
      <div class="container">
        <a class="navbar-brand brand-mark" href="/">
          <span class="brand-dot"></span>
          PulseBridge
        </a>
        <div class="collapse navbar-collapse">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item"><a class="nav-link" href="/events">Events</a></li>
            <li class="nav-item"><a class="nav-link" href="/login">Login</a></li>
          </ul>
        </div>
      </div>
    </nav>

    <section class="py-5">
      <div class="container">
        <div class="row g-4 align-items-stretch">
          <div class="col-lg-6 fade-up">
            <div class="form-panel h-100">
              <h2 class="section-title">Create Account</h2>
              <p class="text-muted">Join the volunteer network and organize impactful events.</p>
              <% if (message) { %>
                <div class="alert alert-success"><%= message %></div>
              <% } %>
              <% if (error) { %>
                <div class="alert alert-danger"><%= error %></div>
              <% } %>
              <form id="registerForm" action="/register" method="POST" novalidate>
                <div class="mb-3">
                  <label class="form-label" for="registerName">Full Name</label>
                  <input id="registerName" name="username" type="text" class="form-control <%= errors.username ? 'is-invalid' : '' %>" placeholder="Alex Johnson" minlength="2" value="<%= values.username || '' %>" required />
                  <div class="invalid-feedback"><%= errors.username || 'Введите имя (минимум 2 символа).' %></div>
                </div>
                <div class="mb-3">
                  <label class="form-label" for="registerEmail">Email</label>
                  <input id="registerEmail" name="email" type="email" class="form-control <%= errors.email ? 'is-invalid' : '' %>" placeholder="alex@example.com" value="<%= values.email || '' %>" required />
                  <div class="invalid-feedback"><%= errors.email || 'Введите корректный email.' %></div>
                </div>
                <div class="mb-3">
                  <label class="form-label" for="registerPassword">Password</label>
                  <input id="registerPassword" name="password" type="password" class="form-control <%= errors.password ? 'is-invalid' : '' %>" placeholder="Create a strong password" minlength="8" required />
                  <div class="invalid-feedback"><%= errors.password || 'Пароль минимум 8 символов.' %></div>
                </div>
                <button type="submit" class="btn btn-coral w-100">Create Account</button>
                <p class="mt-3 mb-0">Already registered? <a href="/login">Go to login</a>.</p>
              </form>
            </div>
          </div>
          <div class="col-lg-6 fade-up fade-delay-1">
            <div class="form-panel h-100">
              <h2 class="section-title">Why PulseBridge?</h2>
              <p class="text-muted">Your account unlocks volunteer coordination, event tracking, and impact posts.</p>
              <ul class="list-unstyled mb-0">
                <li class="mb-2">Organize events with dates, locations, and capacity.</li>
                <li class="mb-2">Track volunteers and shifts in one place.</li>
                <li class="mb-2">Publish recap posts and outcomes.</li>
                <li>Secure access to your profile.</li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </section>

    <footer class="footer">
      <div class="container text-center">
        Create your account to access the PulseBridge platform.
      </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
